{
  "name": "preact-ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "latest"
  },
  "devDependencies": {
    "@preact/preset-vite": "latest",
    "typescript": "latest",
    "vite": "latest"
  }
}
